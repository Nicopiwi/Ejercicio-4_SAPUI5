_schema-version: "3.2"
ID: ejercicio4
version: 0.0.1
modules:
- name: ejercicio4-approuter
  type: approuter.nodejs
  path: ejercicio4-approuter
  requires:
  - name: ejercicio4_html_repo_runtime
  parameters:
    disk-quota: 256M
    memory: 256M
- name: ejercicio4_ui_deployer
  type: com.sap.application.content
  path: .
  requires:
  - name: ejercicio4_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - ejercicio4-content.zip
      name: ejercicio4
      target-path: resources/
- name: ejercicio4
  type: html5
  path: ejercicio4
  build-parameters:
    builder: custom
    commands:
    - npm run build
    supported-platforms: []
resources:
- name: ejercicio4_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: ejercicio4_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm install
